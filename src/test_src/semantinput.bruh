number a is 1.
number b is 2.

define foo(number a -> number) {
  loop a < 5 {
    a is a + 1.
  }

  loop i in 1 to 5 by 0.5 {
    a is a + 1.
  }
}

define bar(number a -> boolean) {
  return a > 5.
}

boolean c is bar(1).
