define gcd(number primes, number count, number cand -> string):
  loop count < primes:
    if cand < 4:
      count is count + 1
      say(cand)
    else:
      number i is 2
      boolean prime is true
      loop i < cand:
        if cand % i == 0:
          prime is false
        i is i + 1
      if prime:
        count is count + 1
        say(cand)
    cand is cand + 1
  
  return "Done"

say(gcd(10, 0, 2))
